name: Legacy Packaging Workflow

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow status
        run: |
          echo "This legacy workflow is disabled."
          echo "Use .github/workflows/semantic-release.yml for automatic release packaging."
